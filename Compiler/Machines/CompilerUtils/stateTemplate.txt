#stateName: {
        _onEnter: async function() {
            for(param in data[this.uuid]["context"].result.parameters)
            {
                data[this.uuid]['store'][param] = data[this.uuid]["context"].result.parameters[param]
            }
            bots[this.uuid] = this;
            #code
        },
        back: function(intent, rootIntent){
            data[this.uuid]["microBots"][rootIntent] = bots[this.uuid]; 
            this.handle(intent)
        },
        //transitions
}